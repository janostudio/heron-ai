name: qa_team
description: "单 Agent 问答团队"

stages:
  - process: sequential
    tasks:
      - name: answer
        agent: assistant
        description: "回答用户的问题：{{.Input}}"
        expected_output: "回答内容"
        output_format: text
